# ── Private business info — NEVER publish (served publicly via GitHub Pages otherwise) ──
CLAUDE.md

# ── Local environment / secret keys ──
.env
.env.*

# ── Old homepage backups — keep locally, do NOT serve publicly ──
index.backup.html
index-old.html
index-new.html

# ── Messy-named original photo (clean copy Image/founder.jpg is the one we publish) ──
Image/Founder.jpg.jpeg

# ── Internal sales scripts & office documents — keep local, NEVER publish ──
showcase/*/sales.html
Sales Script.docx
*.docx
*.xlsx

# ── Dependencies / OS cruft ──
node_modules/
.DS_Store
Thumbs.db
